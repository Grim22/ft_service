Pour relancer un deployment après avoir changer l'image:
kubectl rollout restart deployment/[Nom du deployment]

cmd to access dashboard
minikube dashboard

ssh -p 30010 root@MINIK_IP to connect en ssh
-> port specified in nginx.yaml: 22 not working (blocked ?)
-> access via nodeport